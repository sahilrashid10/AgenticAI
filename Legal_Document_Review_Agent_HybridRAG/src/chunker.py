from typing import List


def chunk_text(text: str, chunk_size: int = 1000, overlap: int = 100) -> List[str]:
    """Placeholder: split text into chunks for embedding."""
    raise NotImplementedError
